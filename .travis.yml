language: node_js

env:
  - DATABASE_URL=postgresql://coder:pg123@localhost:5432/users

before_script:
  - psql -c 'create database  users_tests;' -U postgres
  - psql -U postgres -d users_tests -a -f users.sql
